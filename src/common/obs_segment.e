note
	description: "A section of the OBS_STORABLE object space which will be persisted together"
	date: "$Date: $"
	revision: "$Revision: $"

class
	OBS_SEGMENT

-- A segment edited in windows to create another conflict.

end
